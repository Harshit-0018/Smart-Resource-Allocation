/**
 * RunMatching — admin triggers AI matching for a selected task.
 * Displays task selector, triggers POST /api/match/run/{taskId}, shows results.
 */
export default function RunMatching() {
  // TODO: Implement matching trigger UI with task selector and results display
  return (
    <div>
      <h1 className="text-2xl font-bold">Run AI Matching</h1>
      <p className="text-gray-500 mt-2">Select a task and trigger the matching algorithm.</p>
    </div>
  );
}
